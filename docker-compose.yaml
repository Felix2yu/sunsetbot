services:
  sunsetbot:
    container_name: sunsetbot
    image: sunsetbot:latest
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
